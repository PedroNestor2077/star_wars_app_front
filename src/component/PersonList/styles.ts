import styled from 'styled-components';

export const PersonList = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-between;
    width: 100%;
    row-gap: 110px;
    height: auto;
`;
